// My exercise:

const address = {
  street: "Westbeech",
  city: "London",
  postcode: "N22 6HT",
};

function showAddress(obj) {
  for (let key in obj) {
    console.log(key, obj[key]);
  }
}

showAddress(address);

// Mosh approach:
// The same approach, BRAVO!! (it was very easy)
